interface:
  display_name: "CIPTS Platform"
  short_description: "Repo-specific patterns and workflows for CIPTS-Platform"
  default_prompt: "Use the CIPTS-Platform repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true